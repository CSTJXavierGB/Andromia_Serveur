meta {
  name: [200] Get exploration
  type: http
  seq: 2
}

get {
  url: {{base_url}}/explorations/:explorationUUID
  body: none
  auth: inherit
}

params:path {
  explorationUUID: {{explorationUUID}}
}

assert {
  res.status: eq 200
  res.body: isJson
}