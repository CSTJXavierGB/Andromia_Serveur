meta {
  name: [200] Get exploration ally embed
  type: http
  seq: 3
}

get {
  url: {{base_url}}/explorations/:explorationUUID?embed=ally
  body: none
  auth: inherit
}

params:query {
  embed: ally
}

params:path {
  explorationUUID: {{explorationUUID}}
}

assert {
  res.status: eq 200
  res.body: isJson
  res.body.ally.kernel: isDefined
}
