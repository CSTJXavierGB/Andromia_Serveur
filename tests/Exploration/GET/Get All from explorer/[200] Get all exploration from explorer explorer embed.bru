meta {
  name: [200] Get all exploration from explorer explorer embed
  type: http
  seq: 2
}

get {
  url: {{base_url}}/explorers/:explorerUUID/explorations?embed=explorer
  body: none
  auth: inherit
}

params:query {
  embed: explorer
}

params:path {
  explorerUUID: {{explorerUUID}}
}

assert {
  res.status: eq 200
  res.body: isArray
  res.body[0].explorer.vault: isDefined
}
