meta {
  name: [204] - Logout
  type: http
  seq: 3
}

delete {
  url: {{base_url}}/sessions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{explorerAccessToken}}
}

body:json {
  {
    "refreshToken":"{{explorerRefreshToken}}"
  }
}

assert {
  res.status: eq 204
  res.body: isEmpty
}
